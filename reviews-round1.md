[codex] Starting Codex task thread.
[codex] Thread ready (01a0bd32-69b8-7c53-9ace-ea2e4f39af01).
[codex] Turn started (01a0bd32-6a78-7830-9f5d-9be468054aaa).
[codex] Assistant message captured: 我會對照 SPEC 的進度與驗收規則，檢查三層交接、狀態復原及排課在中斷、重試和補強情境下是否成立。
[codex] Running command: /bin/zsh -lc 'pwd; rg --files -g AGENTS.md -g PLAN.md -g SPEC.md; nl -ba PLAN.md'
[codex] Command completed: /bin/zsh -lc 'pwd; rg --files -g AGENTS.md -g PLAN.md -g SPEC.md; nl -ba PLAN.md' (exit 0)
[codex] Assistant message captured: { "verdict": "needs-attention", "summary": "不建議依現稿開工。三層之間缺少可執行的進度交接，完成課數不足以決定下一課，cron 也無法知道值班...
[codex] Turn completed.
# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

不建議依現稿開工。三層之間缺少可執行的進度交接，完成課數不足以決定下一課，cron 也無法知道值班或停課狀態；備份與教材品質驗收另有實質缺口。

Findings:
- [high] ChatGPT 負責調整，卻沒有把決策送回閱讀器的路徑 (PLAN.md:15-21)
  PLAN 將進度與難度調整交給 ChatGPT，但只設計每週 PWA→ChatGPT 的週報，沒有反向套用機制。依目前設計推論，即使 ChatGPT 判定階段測驗未過、需要補強一週，PWA 仍會按完成課數前進。每日低於 70% 的次日補強也等不到週報。這不只是切換 App 的摩擦，而是違反 SPEC §7.2、§7.3、§8.2 的控制流程。
  Recommendation: 定義雙向交接契約：送出課次、作答與錯誤類型，接回補強任務、階段通過判定及下一課。v1 可人工匯入或確認，不必加後端；每日補強與階段門檻必須在閱讀器落實。若要刪減規則，先明確修訂 SPEC。
- [high] 以完成課數推算下一課，無法表達忙碌版、跳課與補強 (PLAN.md:64-65)
  「開始日＋已完成課數」只能處理從第一課連續完成的情況。SPEC 允許診斷後跳過熟練內容，忙碌版又必須保留未學的新內容；單一計數無法識別下一個待完成的課次。忙碌版若算完成會跳掉新內容，若不算完成，現有單筆 date／busy 狀態也沒有定義如何保留多日練習與後續正式完成紀錄。
  Recommendation: 以明確的課次狀態與下一課選擇規則取代完成數推算，區分未開始、部分完成、正式完成、診斷豁免及待補強；忙碌練習另存紀錄，不推進新課。定義哪些模組與驗收條件滿足後才可前進。
- [high] cron 無法履行「休息日、值班日不生新課」 (PLAN.md:78-82)
  工廠每天執行，唯一排課輸入是上次生成後加一的 next_lesson.txt；手機狀態不回寫，也沒有休息或值班設定。因此三天沒學仍會生成三課並推播，與宣稱的停課行為矛盾。生成超前本身可以接受，但目前把生成課次稱為今日課，又用「昨天的教材」處理缺課，會混淆教材庫最新課與使用者真正待學課。
  Recommendation: 明確選擇預先生成教材庫或依學習需求生成。方案 A 下可採前者：工廠只管理生成游標與 120 課終點，PWA 只按本機學習狀態選課；通知連到 PWA 待學課，缺檔時顯示該課尚未就緒。若要依值班停產，必須補上工廠可讀取的排程來源。
- [high] 週報摘要不是可還原的進度備份 (PLAN.md:70)
  週報只列本週完成課次、分數及弱點，卻同時被指定為匯入／匯出格式。這些資訊不足以重建歷週資料、未完成模組、忙碌練習與開始日。清除網站資料或更換手機後，即使留有最新週報，也無法恢復原進度；建置順序還允許在備份功能完成前開始累積唯一一份 localStorage 紀錄。
  Recommendation: 保留 localStorage 也可以，但另設完整、含版本號的 JSON 備份，涵蓋全部學習狀態與課綱版本；匯入需驗證並明訂覆蓋或合併規則。將完整匯出及還原驗證列為開始正式使用的前置條件，週報只作溝通摘要。
- [medium] 課綱沒有單字清冊，無法保證新詞數量或計算學習涵蓋率 (PLAN.md:28-35)
  curriculum 只有 vocab_theme，卻宣稱能從中算出約 1000 個單字的分母。每日生成只收到前一課單字，因此無從判斷更早教過的詞；重複詞仍可能被當成十個新詞。首頁用完成課數推算單字數也會把 Day6 複習課算入，造成看似達標、實際詞彙涵蓋不足。
  Recommendation: 在課綱配置具穩定 ID 的單字與文法清冊，逐課標記新增及複習項目；生成後驗證實際涵蓋清冊，進度按已完成的唯一項目計算。讓 review／stage_test 使用不同數量規則，避免固定十詞被誤算成新增。
- [medium] 格式驗證通過就發布，沒有攔截錯誤讀音與答案的品質門檻 (PLAN.md:50-51)
  目前發布前只驗數量、必填、kana 字元與 JSON。即使假名完全由合法字元組成，也可能對應錯誤漢字讀音；題目答案也可能與文章矛盾，仍會自動 commit 並提供學習。對初學者而言，這些錯誤難以自行察覺。前置只看 W01D1–W01D3，亦沒有涵蓋後期長文與測驗課。
  Recommendation: 將結構驗證與內容驗收分開：明訂假名欄位允許的標點、空白及長音符，另檢查讀音對應、答案可由教材支持及課綱一致性。正式使用前抽驗各階段與三種 day_type，並提供錯誤教材停用、修訂與版本辨識流程。

Next steps:
- 先補齊下一課選擇、補強與階段驗收的狀態轉移表，以及 ChatGPT 決策回填方式。
- 決定工廠採預生成或需求驅動，移除互相矛盾的今日課與停產假設。
- 以忙碌兩天後恢復、診斷跳課、階段未過、手機資料清空及教材答案錯誤五個情境驗收修訂後的計畫。
